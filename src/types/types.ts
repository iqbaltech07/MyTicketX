export interface IBanners {
  id: number;
  image: string;
  alt: string;
}

export interface IEvents {
  id: string; 
  title: string;
  slug: string;
  description: string;
  thumb: string;
  date: string;
  time: string;
}